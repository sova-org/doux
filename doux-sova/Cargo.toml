[package]
name = "doux-sova"
version = "0.0.1"
edition = "2021"
description = "Sova integration layer for the doux audio engine"
license = "AGPL-3.0-only"
repository = "https://github.com/Bubobubobubobubo/doux"

[features]
default = []
soundfont = ["doux/soundfont"]
asio = ["doux/asio"]

[dependencies]
doux = { path = "..", features = ["native"] }
crossbeam-channel = "0.5"
ringbuf = "0.4"
serde = { version = "1", features = ["derive"] }
sova_core = { git = "https://github.com/sova-org/sova", package = "core" }
